// Shader for the brain rendered with a cloud of points
#include "Generic_Parameters.cg"

void fragment_shader(in float4 color : COLOR, out float4 oColor : COLOR)
{

  if (color.a < 0.01f)
  {
	discard;
  }
  
  oColor = color;
}